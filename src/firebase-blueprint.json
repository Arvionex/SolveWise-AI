{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["free", "premium", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "displayName", "role", "createdAt"]
    },
    "Problem": {
      "title": "Problem",
      "description": "A problem submitted by a user",
      "type": "object",
      "properties": {
        "user_id": { "type": "string" },
        "problem_text": { "type": "string" },
        "category": { "type": "string" },
        "ai_solution": { "type": "string" },
        "is_public": { "type": "boolean" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["user_id", "problem_text", "category", "ai_solution", "is_public", "timestamp"]
    },
    "Donation": {
      "title": "Donation",
      "description": "A donation made by a user",
      "type": "object",
      "properties": {
        "user_id": { "type": "string" },
        "displayName": { "type": "string" },
        "amount": { "type": "number" },
        "message": { "type": "string" },
        "payment_id": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["amount", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profiles"
    },
    "/problems/{problemId}": {
      "schema": "Problem",
      "description": "Problems and solutions"
    },
    "/donations/{donationId}": {
      "schema": "Donation",
      "description": "Donations for the platform"
    }
  }
}
